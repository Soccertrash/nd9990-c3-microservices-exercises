apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend
  name: frontend-svc
spec:
  ports:
  - name: http
    port: 8100
    targetPort: 8100
    protocol: TCP
  selector:
    app: frontend
  type: ClusterIP
status:
  loadBalancer: {}
